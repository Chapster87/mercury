/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
        'postcss-will-change': {},
        'postcss-calc': {},
        'postcss-flexbugs-fixes': {},
        autoprefixer: {}
    }
};

module.exports = config;
